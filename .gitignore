__pycache__/
*.pyc

# Modelul de vehicul cu camera depinde de config/camera_pi.yaml, care nu
# exista pana la calibrarea reala. Commit-uit, ar contine intrinseci
# inventati si nimeni nu ar observa. Se genereaza cu
# tools/make_camera_model.py dupa calibrare.
sim/models/iris_nova/

# Iesirea campaniilor: lumi generate, loguri, CSV-uri, cadre. Se
# regenereaza din plan (`--seed`), deci nu are ce cauta in istorie. Ce se
# pastreaza ca evidenta se copiaza explicit, cu tools/collect_session.py.
data/batch/
claude-markdown

# CONFIDENTIALE. Atentie: gitignore NU scoate un fisier deja urmarit -
# un fisier listat aici dar commit-uit inainte ramane in istorie pana e
# scos explicit (git rm --cached + curatarea istoriei). Tiparele previn
# clasa de greseala, nu doar un nume.
ims-backup.key
*.key
*.pem
*.p12
.env
.env.*
id_ed25519*
id_rsa*
*.secret
